export default function Setup(){return <main className="evaluation"><a href="/">← Back to Swayam</a><p className="eyebrow">BUILDER WORKSPACE</p><h1>Connect. Test. Finish.</h1><p>Swayam is a TypeScript / React application with a server backend. Its code is the project supplied with your setup guide. API keys belong only in server settings.</p><h2>1. Run the code locally</h2><p>Install Node.js 22.13 or newer and pnpm 11.25.0. Open the extracted project folder in VS Code or Cursor.</p><pre>pnpm install{`\n`}pnpm dev</pre><p>Open the local address printed by the terminal. Without API keys, the 13 prepared guides still work.</p><h2>2. Add server settings</h2><p>Create <code>.dev.vars</code> in the project root using <code>.env.example</code> as the template. Restart the server after editing. Never paste keys into chat, screenshots, source code or a public repository.</p><div className="test-cases">{[['FIREWORKS_API_KEY','Secret · Fireworks API key'],['FIREWORKS_MODEL','Exact model ID available to your Fireworks account'],['YOU_API_KEY','Secret · You.com Search API key; a consumer subscription may not include API credits'],['ELEVENLABS_API_KEY','Secret · ElevenLabs key with speech-to-text and text-to-speech access'],['ELEVENLABS_VOICE_ID','A voice available to your ElevenLabs account; test Telugu pronunciation']].map(([name,help])=><article key={name}><strong>{name}</strong><p>{help}</p></article>)}</div><p>Local settings do not automatically configure the hosted site. The hosted version needs the same server values in its environment settings and a new deployment. This page never displays or accepts keys.</p><h2>3. Check one real answer first</h2><p>Ask “కాగితంతో పడవ ఎలా చేయాలి?” This is outside the shortcuts. Check every source, try a follow-up, then listen. Next try LPG KYC, a legal-awareness question with your state, and a recipe. If sources are insufficient, the correct result is an honest limitation.</p><h2>4. Capture Week 6 evidence</h2><p><a href="/evaluation">Open Project tests</a>. Run the guided suite, then the 16 web-answer cases after configuration. Download each run before running the next one. Generated answers start as WARN pending human review. Save important screenshots without keys or real personal data.</p><pre>node scripts/evaluate.mjs{`\n`}node scripts/evaluate-answers.mjs</pre><p>The first command tests guided controls. The second uses simulated providers; neither proves live model resistance.</p><h2>5. Complete the submission</h2><p>Use Path B: red-team your own agent. The Google Doc needs prompts, exact responses, evidence, PASS/WARN/FAIL and reasons. Include the code and defense mapping because this project implements controls. Add live-provider results and actual phone voice checks; do not mark unrun work as complete.</p><p><a href="https://forms.gle/5cHmQJGxdC3X3Lrh8" target="_blank" rel="noopener noreferrer">Week 6 submission form</a> · Deadline: September 20, 2026, 11:59 PM Pacific.</p></main>}
